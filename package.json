{
  "name": "boot-root",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo root for Gambling Affiliate Manager (Next.js Dashboard + NestJS Backend)",
  "scripts": {
    "build": "npm --prefix backend install && npm --prefix backend run build",
    "start": "npm --prefix backend run start:prod",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "install:all": "npm --prefix backend install && npm --prefix dashboard install",
    "db:push": "cd backend && npx prisma db push",
    "dev:backend": "npm --prefix backend run start:dev",
    "dev:dashboard": "npm --prefix dashboard run dev",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:dashboard\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
